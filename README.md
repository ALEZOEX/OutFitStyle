# 👔 OutfitStyle

Умное приложение для рекомендаций одежды с ML персонализацией.

## 🏗️ Архитектура системы

```
outfitstyle/
├── client/                  # Flutter клиент
├── server/                  # Go REST API (8080)
│   ├── cmd/                 # Точка входа приложения
│   ├── internal/            # Внутренние пакеты
│   │   ├── api/             # HTTP слой (middleware, handlers)
│   │   ├── core/            # Бизнес-логика (domain, services)
│   │   ├── infrastructure/  # Внешние зависимости (БД, HTTP клиенты)
│   │   └── pkg/             # Общие утилиты
│   ├── migrations/          # Миграции базы данных
│   ├── deployments/         # Docker и Kubernetes конфигурации
│   ├── scripts/             # Скрипты для разработки
│   ├── .env.example         # Пример конфигурации
│   ├── Makefile             # Сборка и запуск
│   ├── go.mod               # Зависимости Go
│   └── go.sum               # Контрольные суммы зависимостей
├── ml-service/              # Python ML Service (5000)
├── marketplace-service/     # Flask marketplace сервис
└── docker-compose.yml       # Оркестрация сервисов
```

## 🚀 Запуск

### Docker (рекомендуется)
```bash
# Запуск всех сервисов
docker-compose up --build

# Запуск в фоновом режиме
docker-compose up -d --build

# Запуск в продакшене
docker-compose -f docker-compose.prod.yml up --build
```

### Ручной запуск

#### База данных
```bash
# Запуск PostgreSQL

# Инициализация базы данных
```

### Ручной запуск

#### База данных
```bash
# Запуск PostgreSQL
docker-compose up -d postgres

# Инициализация базы данных
psql -h localhost -U Admin -d outfitstyle -f server/database/init.sql
```

#### API (Go)
```bash
cd server
make run
```

#### ML Сервис (Python)
```bash
cd ml-service
pip install -r requirements.txt
python main.py
```

#### Клиент (Flutter)
```bash
cd client
flutter run
```

## 📚 Технологии

### Сервер:
- **Go 1.21** (REST API)
- **Python 3.11** (ML сервис)
- **PostgreSQL 15** (База данных)
- **Redis** (Кэширование)
- **Docker** (Контейнеризация)
- **Kubernetes** (Оркестрация)
- **Prometheus** (Метрики)
- **Gorilla Mux** (Маршрутизация)
- **JWT** (Аутентификация)

### Клиент:
- **Flutter 3.16** (iOS, Android, Web)

## 🎯 Фичи

- 🌤 **Реальная погода** (OpenWeatherMap)
- 🤖 **ML персонализация** (Gradient Boosting)
- 📊 **История рекомендаций**
- ⭐ **Система оценок**
- 👤 **Профили пользователей**
- 📈 **Обучение на отзывах**
- 🎯 **Уверенность рекомендаций** (AI confidence scores)
- 📊 **Визуализация уверенности** (Confidence rings & progress bars)
- 🔐 **JWT аутентификация**
- 📈 **Метрики Prometheus**
- 🛡️ **Rate limiting**
- 🔄 **Graceful shutdown**
- 🐳 **Docker поддержка**
- ☸️ **Kubernetes поддержка**

## 🗄️ База данных

### Структура:
- `users` - Пользователи
- `user_profiles` - Профили пользователей
- `clothing_items` - Предметы одежды (200+ реальных предметов)
- `recommendations` - Рекомендации
- `recommendation_items` - Связь рекомендаций с предметами
- `ratings` - Оценки пользователей
- `usage_history` - История взаимодействий

### Инициализация:
```sql
-- Создание тестовых данных
INSERT INTO users (email, name) VALUES 
('alex@example.com', 'Alex'),
('maria@example.com', 'Maria'),
('john@example.com', 'John');
```

## 🧠 ML Сервис

### Алгоритмы:
- **Gradient Boosting Classifier** (основной алгоритм)
- **Random Forest Classifier** (альтернативный алгоритм)
- **Rule-based система** (резервный вариант)
- **Neural Network** (экспериментальный)

### Возможности:
- Обучение на пользовательских оценках
- Персонализированные рекомендации
- Предсказание подходящей одежды
- Адаптация к предпочтениям пользователя
- Объяснение рекомендаций
- **Уверенность в рекомендациях** (Confidence scores 0-100%)
- **Анализ важности признаков** (Feature importance analysis)
- **Визуализация уверенности** (Confidence visualization in UI)
- **A/B тестирование моделей**
- **Автоматическое переобучение**

### API эндпоинты:
```
POST /api/ml/recommend      # Получить рекомендации с уверенностью
POST /api/ml/train          # Переобучить модель
POST /api/ml/rate           # Оценить рекомендацию
POST /api/ml/predict-single # Предсказать для одного предмета
GET  /api/ml/stats          # Статистика системы
GET  /api/ml/model-info     # Информация о модели
GET  /health                # Проверка состояния
```

### Извлечение признаков:
- **Погодные признаки**: температура, влажность, скорость ветра, погодные условия, сезон
- **Пользовательские признаки**: чувствительность к температуре, предпочтения по стилю, возрастная группа, формальность
- **Признаки предметов**: уровень тепла, уровень формальности, температурный диапазон, категория, стиль
- **Признаки взаимодействия**: совместимость температуры, соответствие предпочтениям пользователя
- **Географические признаки**: широта, долгота, климатический регион
- **Временные признаки**: время суток, день недели, праздники

## 📱 Клиент (Flutter)

### Возможности:
- Просмотр погоды в реальном времени
- Получение рекомендаций одежды
- Оценка рекомендаций
- История рекомендаций
- Профиль пользователя
- **Визуализация уверенности AI** (Confidence rings, progress bars)
- **Интерактивные карточки предметов** (Tap animations)
- **Детальная информация о рекомендациях** (Bottom sheet modals)

### Поддерживаемые платформы:
- iOS
- Android
- Web

## 🔧 Конфигурация

### Переменные окружения:
```bash
# API
PORT=8080
WEATHER_API_KEY=your_openweather_api_key

# База данных
DB_HOST=localhost
DB_PORT=5432
DB_USER=Admin
DB_PASSWORD=password
DB_NAME=outfitstyle
```

## 📈 Обучение модели

### Процесс:
1. Пользователи получают рекомендации
2. Пользователи оценивают рекомендации
3. Система собирает данные об оценках
4. ML сервис переобучается на новых данных
5. Качество рекомендаций улучшается

### Требования:
- Минимум 50 оценок для первого обучения (повышено с 20)
- Регулярное переобучение для улучшения точности
- **Автоматическое обучение при запуске** (если модель отсутствует)

## 🧪 Тестирование

### Проверка интеграции:
```bash
cd server/ml-service
python test_integration.py
```

### Ручное тестирование API:
```bash
# Проверка состояния
curl http://localhost:5000/health

# Получение рекомендаций
curl -X POST http://localhost:5000/api/ml/recommend \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 1,
    "weather": {
      "location": "Moscow",
      "temperature": 5,
      "feels_like": 3,
      "weather": "Облачно",
      "humidity": 70,
      "wind_speed": 5
    }
  }'

# Информация о модели
curl http://localhost:5000/api/ml/model-info
```