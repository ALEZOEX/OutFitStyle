name: Frontend CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'client/**'
      - 'k8s/frontend.yaml'
  pull_request:
    branches: [main]
    paths:
      - 'client/**'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/frontend

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Ensure lowercase repository name
        run: |
          echo "IMAGE_NAME_LOWERCASE=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWERCASE }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=,format=short
            type=ref,event=pr

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./client
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}
          if_key_not_found: 'error'

      - name: Deploy to k3s
        run: |
          ssh -o StrictHostKeyChecking=no root@144.31.234.69 << 'EOF'
            set -e
            echo "🔄 Updating frontend deployment..."
            kubectl rollout restart deployment/frontend -n outfitstyle
            echo "⏳ Waiting for rollout..."
            kubectl rollout status deployment/frontend -n outfitstyle --timeout=120s
            echo "✅ Frontend updated successfully!"
            echo "📊 Pod status:"
            kubectl get pods -n outfitstyle -l app=frontend
          EOF

  cleanup-old-images:
    needs: deploy
    runs-on: ubuntu-latest
    if: always() && needs.deploy.result == 'success'
    permissions:
      packages: write
    steps:
      - name: Delete old frontend images
        uses: bots-house/ghcr-delete-image-action@v1.1.0
        with:
          owner: alezoex
          name: outfitstyle/frontend
          token: ${{ secrets.GITHUB_TOKEN }}
          untagged-ages: 1
          keep-latest: 3
